[pytest]
console_output_style = classic
faulthandler_timeout=5
addopts = --maxfail=1 -rf -s
log_cli=true  
log_level=INFO
log_format = %(asctime)s %(levelname)s %(message)s  
log_date_format = %Y-%m-%d %H:%M:%S
log_file = logs/pytest-logs.txt
pythonpath=.
testpaths=
    tests
python_files = test_*.py